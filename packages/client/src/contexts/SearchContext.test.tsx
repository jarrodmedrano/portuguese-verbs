import { describe, it, expect } from 'vitest';
import { SearchContextProvider } from './SearchContext';

describe('SearchContext', () => {
  it('should be defined', () => {
    expect(SearchContextProvider).toBeDefined();
  });
});
