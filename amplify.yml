version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yum -y install jq
        - jq --version
        - nvm install 16
        - yarn install
    build:
      commands:
        - bash scripts/before_install.sh
        - yarn run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
